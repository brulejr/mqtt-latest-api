info:
  name: Config
  type: http
  seq: 2

http:
  method: GET
  url: "{{mqtt_latest_api_url}}/api/config"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
