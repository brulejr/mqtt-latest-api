info:
  name: Get Latest Items
  type: http
  seq: 1

http:
  method: GET
  url: "{{mqtt_latest_api_url}}/api/latest"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
